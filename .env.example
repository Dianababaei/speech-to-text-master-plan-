# Database Configuration
# ======================

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/transcription_db

# Connection Pool Settings
# -------------------------

# Number of persistent connections in the pool (default: 5)
# Suitable for small scale applications
DB_POOL_SIZE=5

# Maximum additional connections beyond pool_size (default: 10)
# Allows burst capacity for temporary spikes in traffic
DB_MAX_OVERFLOW=10

# Recycle connections after this many seconds (default: 3600)
# Prevents issues with long-lived connections
DB_POOL_RECYCLE=3600

# Test connections before using (default: true)
# Prevents stale connection errors
DB_POOL_PRE_PING=true

# Development Settings
# --------------------

# Log all SQL queries (default: false)
# Set to true for development/debugging, false for production
DB_ECHO=false

# Application environment (default: dev)
# Options: dev, test, production
ENVIRONMENT=dev
# Copy this file to .env and update with your actual values

# PostgreSQL Database Settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=speech_to_text
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# Alternative: Use DATABASE_URL (takes precedence over individual variables)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/speech_to_text

# Docker Settings
AUTO_MIGRATE=true  # Set to false to disable automatic migrations on container startup

# Application Settings (for future use)
# APP_ENV=development
# LOG_LEVEL=INFO
# API_KEY=your_openai_api_key_here
